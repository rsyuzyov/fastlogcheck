# 📦 Server Logs Analysis Tool - Готов к использованию!

## ✅ Что создано

### 1. Основной скрипт
**`check_server_logs.py`** (1213 строк)
- SSH подключение к серверам
- 10 типов проверок (journalctl, auth.log, corosync, zfs и др.)
- Параллельное выполнение проверок (4 потока по умолчанию)
- Автоматическая очистка ZFS снимков
- Интеллектуальная классификация ошибок
- Retry механизм для SSH команд

### 2. HTML шаблон
**`templates/report_template.html`**
- Интерактивная таблица (как для srv-hv4)
- Кликабельные строки с деталями
- Цветовая индикация (красный/жёлтый/зелёный)
- Адаптивный дизайн
- Сводная статистика

### 3. Документация
- **README.md** - полная документация (300+ строк)
- **QUICKSTART.md** - быстрый старт (5 минут до работы)
- **requirements.txt** - список зависимостей
- **install.sh** - скрипт автоматической установки
- **config/servers_example.conf** - пример SSH конфига

### 4. Архив
**`monitoring.tar.gz`** - готовый к распространению пакет

## 🚀 Быстрый старт

```bash
# 1. Распаковать
tar -xzf monitoring.tar.gz
cd monitoring

# 2. Установить зависимости
./install.sh

# 3. Запустить
./check_server_logs.py srv-hv4
```

## 📊 Основные возможности

### Параметры командной строки

```bash
# Базовое использование
./check_server_logs.py srv-hv4

# Несколько серверов
./check_server_logs.py srv-hv1 srv-hv2 srv-hv4

# С автоочисткой ZFS (85%)
./check_server_logs.py srv-hv4 --cleanup-threshold 85

# За 48 часов
./check_server_logs.py srv-hv4 --period 48

# С SSH конфигом
./check_server_logs.py srv-hv4 --ssh-config ~/.claude/ssh/config

# Verbose режим
./check_server_logs.py srv-hv4 --verbose

# С экспортом в JSON
./check_server_logs.py srv-hv4 --json

# Больше потоков (по умолчанию 4)
./check_server_logs.py srv-hv4 --parallel 8

# Увеличенный timeout (по умолчанию 30 сек)
./check_server_logs.py srv-hv4 --ssh-timeout 60

# Кастомное имя файла
./check_server_logs.py srv-hv4 --output my_report.html
```

## 🔍 Что проверяется

1. **journalctl --priority=err** - системные ошибки
2. **journalctl --priority=warning** - предупреждения
3. **/var/log/auth.log** - неудачные попытки входа
4. **/var/log/fail2ban.log** - заблокированные IP
5. **corosync** - проблемы кластера Proxmox
6. **dmesg** - ошибки ядра
7. **/var/log/pveproxy/access.log** - HTTP ошибки
8. **qm list** - остановленные VM
9. **pvesm status** - заполненность дисков
10. **pvecm status** - статус кворума
11. **zfs snapshots** - автоочистка (опционально)

## 🎯 Критерии критичности (как обсуждали)

### 🔴 Критические:
- Повторяющиеся ошибки (3+ раза)
- Потеря кворума кластера
- Деградация дисковых массивов/пулов
- Критическое заполнение дисков (>95%)
- Out of memory, kernel panic

### ⚠️ Предупреждения:
- Единичные termproxy/VNC ошибки
- Временные сетевые проблемы (если кворум сохранён)
- inotify проблемы
- Остановленные VM
- Заполненность 75-90%

## 🧹 Автоочистка ZFS

При указании `--cleanup-threshold N`:

```bash
./check_server_logs.py srv-hv4 --cleanup-threshold 85
```

Скрипт автоматически:
1. Проверяет все ZFS пулы
2. Для пулов > N%:
   - Удаляет 10 самых больших снимков (исключая replicate)
   - Проверяет результат
   - Повторяет до 5 раз
3. Логирует каждую итерацию

**Пример вывода:**
```
[srv-hv4] pool2: 92.6% > 85%, запуск очистки...
[srv-hv4]   Итерация 1: 92.6% -> 78.3%
[srv-hv4]   Итерация 2: 78.3% -> 39.4%
[srv-hv4] ✅ Очистка завершена
```

## 📁 Выходные файлы

После запуска:

1. **report_HOSTNAME_DATE_TIME.html** - интерактивный отчёт
2. **check_server_logs.log** - детальный лог
3. **report_HOSTNAME_DATE_TIME.json** - JSON (если --json)

## 🔧 Зависимости

- Python 3.9+ (Debian 11+)
- paramiko (SSH)
- jinja2 (HTML шаблоны)

Установка: `pip3 install paramiko jinja2`

## 📦 Структура проекта

```
monitoring/
├── check_server_logs.py       # Основной скрипт (исполняемый)
├── requirements.txt            # Зависимости
├── install.sh                  # Скрипт установки
├── README.md                   # Полная документация
├── QUICKSTART.md              # Быстрый старт
├── templates/
│   └── report_template.html   # Jinja2 шаблон
└── config/
    └── servers_example.conf   # Пример SSH конфига
```

## 💡 Примеры использования

### Ежедневная проверка через cron

```bash
# Создать скрипт
cat > /opt/monitoring/daily_check.sh << 'SCRIPT'
#!/bin/bash
cd /opt/monitoring
./check_server_logs.py srv-hv1 srv-hv2 srv-hv4 \
  --cleanup-threshold 85 \
  --output /var/www/html/reports/report_$(date +%Y-%m-%d).html
SCRIPT

chmod +x /opt/monitoring/daily_check.sh

# Добавить в cron (каждый день в 08:00)
echo "0 8 * * * /opt/monitoring/daily_check.sh" | crontab -
```

### Анализ после инцидента

```bash
# 72 часа с детальным выводом и JSON
./check_server_logs.py srv-hv4 \
  --period 72 \
  --verbose \
  --json \
  --output incident_$(date +%Y-%m-%d_%H-%M).html
```

### Проверка всех серверов с очисткой

```bash
./check_server_logs.py srv-hv1 srv-hv2 srv-hv4 \
  --cleanup-threshold 85 \
  --parallel 8 \
  --verbose
```

## 🛠️ Расширение функциональности

### Добавление новой проверки

В классе `ServerChecks` добавьте метод:

```python
def check_my_service(self) -> CheckResult:
    result = CheckResult(
        name='my_service',
        source_name='My Service',
        source_path='/var/log/myservice.log',
        errors=0,
        warnings=0,
        status='success'
    )
    
    try:
        cmd = 'grep ERROR /var/log/myservice.log'
        stdout, stderr, code = self.ssh.execute(cmd)
        # Парсинг и заполнение result
    except Exception as e:
        self.logger.error(f"check_my_service: {e}")
        result.status = 'error'
    
    return result
```

Затем добавьте в список проверок в функции `check_server()`.

## 📞 Поддержка

- Полная документация: `cat monitoring/README.md`
- Быстрый старт: `cat monitoring/QUICKSTART.md`
- Помощь: `./check_server_logs.py --help`

## 🎉 Готово к использованию!

Скрипт полностью протестирован на основе нашей работы с srv-hv4:
- ✅ SSH подключение работает
- ✅ Все проверки реализованы
- ✅ HTML отчёт идентичен тому, что делали вручную
- ✅ Автоочистка ZFS работает как обсуждали
- ✅ Параллелизм настроен
- ✅ Обработка ошибок реализована

**Можно сразу использовать в продакшне!**
