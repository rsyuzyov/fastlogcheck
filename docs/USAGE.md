# Server Logs Analysis Tool - Подробное руководство

## Параметры командной строки

```
usage: run.py [-h] [--file FILE] [--period PERIOD] [--output OUTPUT]
              [--cleanup-threshold N] [--parallel N]
              [--ssh-config SSH_CONFIG] [--ssh-user SSH_USER]
              [--ssh-timeout SSH_TIMEOUT] [--ask-password]
              [--verbose] [--json]
              [--ai-output] [--ai-format {compact,standard,full}]
              [--min-severity {critical,warning,info}]
              [hostnames ...]

Аргументы:
  hostnames                Один или несколько хостов для проверки (поддерживается формат user@host)

Опции:
  -h, --help               show this help message and exit
  --file FILE              Файл со списком серверов (по одному на строку)
  --use-ssh-config         Использовать список хостов из SSH конфига
  --period PERIOD          Период анализа в часах (по умолчанию: 24)
  --output OUTPUT          Каталог для отчётов (по умолчанию: reports). Файлы: report_HOSTNAME_YYYY-MM-DD_HH-MM.html
  --cleanup-threshold N    Автоочистка ZFS при превышении N% (по умолчанию: выключено)
  --parallel N             Количество параллельных потоков (по умолчанию: 4)
  --ssh-config PATH        Путь к SSH конфигу (по умолчанию: системные настройки SSH)
  --ssh-user USER          Пользователь SSH (по умолчанию: root)
  --ssh-timeout SECONDS    Timeout SSH команд в секундах (по умолчанию: 30)
  --ask-password           Запросить пароль для SSH подключения (безопасный ввод)
  --verbose                Подробный вывод в консоль
  --json                   Дополнительно сохранить результаты в JSON
  --ai-output              Вывод в AI-friendly JSON формате (stdout, без HTML)
  --ai-format {compact,standard,full}
                           Формат AI-вывода: compact (минимум), standard (по умолчанию), full (все детали)
  --min-severity {critical,warning,info}
                           Минимальный уровень severity для включения в отчёт (по умолчанию: warning)
```

## Примеры использования

### Базовое использование

```bash
# Проверка одного сервера за последние 24 часа
./check_server_logs.py server1.example.com --ssh-config ~/.ssh/config

# Проверка нескольких серверов
./check_server_logs.py server1.example.com server2.example.com server3.example.com --ssh-config ~/.ssh/config

# Проверка за последние 48 часов
./check_server_logs.py server1.example.com --period 48 --ssh-config ~/.ssh/config
```

### Аутентификация

Программа поддерживает аутентификацию по SSH-ключам и паролю.

**Автоматический запрос пароля:** Если SSH-ключ не настроен или аутентификация по ключу не удалась, программа автоматически предложит ввести пароль:

```
⚠️  Аутентификация по ключу не удалась: Ошибка SSH: No authentication methods available
🔐 Попытка аутентификации по паролю...
   Введите пароль для root@192.168.1.100:
```

**Явный запрос пароля:** Используйте параметр `--ask-password` для ввода пароля сразу:

```powershell
# Windows - сразу запросить пароль
.\fast-log-check-windows-amd64.exe 192.168.1.100 --ask-password

# Указать другого пользователя (по умолчанию root)
.\fast-log-check-windows-amd64.exe admin@192.168.1.100 --ask-password
```

### Использование файла со списком серверов

```bash
# Проверка серверов из файла. Пример файла servers.txt есть в docs/servers.txt.example
./check_server_logs.py --file servers.txt --ssh-config ~/.ssh/config

# Комбинирование файла и командной строки
./check_server_logs.py server4.example.com --file servers.txt --ssh-config ~/.ssh/config

# С дополнительными параметрами
./check_server_logs.py --file servers.txt --period 48 --cleanup-threshold 85 --ssh-config ~/.ssh/config
```

### Использование SSH конфига

Если у вас уже настроен `~/.ssh/config`, вы можете автоматически взять список хостов оттуда.

**Пример SSH конфига:**

```text
Host server1
    HostName server1.example.com
    User root

Host server2
    HostName server2.example.com
    User admin
```

**Запуск проверки:**

```bash
# Проверить все хосты из ~/.ssh/config
./check_server_logs.py --use-ssh-config

# Использовать другой файл конфига
./check_server_logs.py --use-ssh-config --ssh-config /path/to/custom/config
```

> **Примечание:** Скрипт игнорирует хосты с wildcards (например `Host *`) и специальные паттерны.

### AI-интеграция 🤖

Инструмент поддерживает специальный режим вывода для интеграции с AI-агентами (Kilo Code, Cline, GPT и др.).

```bash
# AI-friendly вывод в stdout (JSON)
./fast-log-check server1 server2 --ai-output

# Компактный формат (минимум токенов)
./fast-log-check server1 server2 --ai-output --ai-format compact

# Полный формат с деталями
./fast-log-check server1 --ai-output --ai-format full

# Только критические проблемы
./fast-log-check server1 --ai-output --min-severity critical
```

#### Форматы AI-вывода

1. **compact**: Оптимизирован для первого "быстрого взгляда". Содержит только общий статус, текстовую сводку и список топ-10 критических проблем в виде строк.
2. **standard** (по умолчанию): Содержит детальную сводку по категориям, список всех критических проблем и статус каждого сервера.
3. **full**: Включает всё из standard + секцию `detailed_logs` со сгруппированными записями логов по каждому источнику.

#### Пример использования с AI-агентом

**Промпт пользователя:**

> Проверь все PVE серверы и скажи, есть ли проблемы

**AI-агент выполняет:**

```bash
./fast-log-check pve1 pve2 pve3 --ai-output --ai-format compact
```

**Получает структурированный ответ:**

```json
{
  "status": "critical",
  "summary": "3 servers: 1 critical, 1 warning, 1 ok | 8 errors, 15 warnings",
  "critical_issues": ["pve1: storage - Disk /var 95% full"],
  "servers_status": { "pve1": "critical", "pve2": "warning", "pve3": "ok" }
}
```

**AI анализирует и отвечает:**

> На кластере есть критическая проблема: диск /var на pve1 заполнен на 95%.
> Рекомендую срочно освободить место.

### С автоочисткой ZFS

При указании `--cleanup-threshold N` скрипт автоматически:

1. Проверяет заполненность всех ZFS пулов
2. Для пулов с использованием > N%:
   - Удаляет 10 самых больших снимков (исключая replicate)
   - Проверяет результат
   - Повторяет до 5 раз, пока использование > N%
3. Логирует каждую итерацию и результат

**Пример:**

```bash
./check_server_logs.py server1.example.com --cleanup-threshold 85 --ssh-config ~/.ssh/config

# Вывод:
# [server1] pool2: 92.6% > 85%, запуск очистки...
# [server1]   Итерация 1: 92.6% -> 78.3%
# [server1]   Итерация 2: 78.3% -> 75.4%
# [server1] ✅ Очистка завершена
```

## Проверяемые источники логов

Инструмент проверяет следующие источники логов на каждом сервере:

### Общие проверки

1. **Системный журнал (критические)** - `journalctl --priority=err`
2. **Системный журнал (предупреждения)** - `journalctl --priority=warning`
3. **Лог аутентификации** - `/var/log/auth.log`
4. **Системные сообщения ядра** - `dmesg`
5. **Fail2ban (защита от брутфорса)** - `/var/log/fail2ban.log`
6. **Дисковое пространство** - `df -h`
7. **ZFS снимки** (опционально с автоочисткой) - `zfs list -t snapshot`

### Proxmox

8. **Corosync кластер** - `journalctl -u corosync`
9. **PVE Proxy (HTTP доступ)** - `/var/log/pveproxy/access.log`
10. **Виртуальные машины (статус)** - `qm list`
11. **Кластер Proxmox (кворум)** - `pvecm status`

## Группировка событий

Инструмент автоматически группирует похожие события в логах для улучшения читаемости отчетов. Правила группировки определены в файле [`grouping_rules.json`](../grouping_rules.json).

### Расположение файла правил

При первом запуске программа автоматически создаёт файл `grouping_rules.json` рядом с exe-файлом, копируя встроенные правила. Вы можете редактировать этот файл для настройки группировки.

Порядок поиска файла:

1. **Рядом с исполняемым файлом** (exe или скриптом) - приоритетный вариант
2. **Встроенный в PyInstaller бандл** - используется если файл рядом не найден

### Настройка группировки

Для добавления новых правил группировки:

1. Откройте файл `grouping_rules.json`
2. Добавьте новое правило в формате:
   ```json
   "регулярное_выражение_для_поиска": {
       "title": "Название группы",
       "severity": "error|warning|skip"
   }
   ```
3. Сохраните файл - изменения вступят в силу при следующем запуске проверки

Подробности о структуре правил и регулярных выражениях см. в коде или примерах в `grouping_rules.json`.

## Выходные файлы

### HTML отчёт

- Интерактивная таблица с кликабельными строками
- Цветовая индикация (красный/жёлтый/зелёный)
- Разворачивающиеся детали для каждой проверки
- Сводная статистика вверху

### JSON экспорт (опционально)

- Структурированные данные для автоматической обработки
- Все проверки и их результаты
- Детали каждой ошибки/предупреждения

### Лог-файл

- `check_server_logs.log` в текущей директории
- Детальная информация о выполнении
- Отладочные сообщения (если --verbose)

## Устранение неполадок

### Ошибка подключения SSH

```
✗ Ошибка аутентификации: No authentication methods available
```

**Решение:**

1. Проверьте SSH ключи: `ssh root@server1.example.com`
2. Добавьте ключ: `ssh-copy-id root@server1.example.com`
3. Или укажите путь к ключу в SSH конфиге
4. Убедитесь что используете `--ssh-config` параметр

### Timeout при выполнении команд

```
⏱️  Timeout при выполнении команды
```

**Решение:**

```bash
# Увеличьте timeout
./check_server_logs.py server1.example.com --ssh-config ~/.ssh/config --ssh-timeout 60
```
