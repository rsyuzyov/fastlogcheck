# Python Auto-Installer для Windows

Универсальные скрипты для автоматической установки Python на Windows системах. Этот модуль можно использовать в любых проектах, требующих Python.

## 🚀 Быстрый старт

### Standalone использование

```cmd
REM Batch версия
install-python.bat

REM PowerShell версия
powershell -ExecutionPolicy Bypass -File install-python.ps1
```

### Интеграция в свой проект

1. Скопируйте каталог `python-installer` в свой проект
2. Вызовите скрипт из вашего install-скрипта:

**Из Batch:**
```batch
REM Проверка Python
python --version > nul 2>&1
if errorlevel 1 (
    echo Python не найден, запуск автоустановки...
    set "CALLED_FROM_PARENT=1"
    call "%~dp0python-installer\install-python.bat"

    if errorlevel 1 (
        echo Автоустановка не удалась
        exit /b 1
    )
)
```

**Из PowerShell:**
```powershell
# Проверка Python
try {
    $null = & python --version 2>&1
    if ($LASTEXITCODE -ne 0) { throw }
} catch {
    Write-Host "Python не найден, запуск автоустановки..."
    $env:CALLED_FROM_PARENT = "1"
    & "$PSScriptRoot\python-installer\install-python.ps1"

    if ($LASTEXITCODE -ne 0) {
        Write-Host "Автоустановка не удалась"
        exit 1
    }
}
```

## ✨ Возможности

- ✅ **Автоматическое определение архитектуры** (x86, x64, ARM64)
- ✅ **Комбинированный подход установки** (winget → официальный установщик)
- ✅ **UAC elevation** - автоматический запрос прав администратора
- ✅ **Откат при ошибке** - автоматическая деинсталляция при сбое
- ✅ **Прогресс установки** - информативные сообщения на каждом шаге
- ✅ **Безопасность** - скачивание только с python.org через HTTPS (TLS 1.2+)
- ✅ **Универсальность** - работает как standalone, так и как модуль

## 📋 Требования

### Системные требования
- **ОС:** Windows 10 (1809+), Windows 11, Windows Server 2019+
- **Архитектура:** x86, x64, ARM64
- **Права:** Администратор (для установки для всех пользователей)
- **Свободное место:** ~200 МБ (Python + зависимости)
- **Интернет:** Требуется для скачивания установщика

### Сетевые требования
- Доступ к `https://www.python.org`
- Протокол: HTTPS (TLS 1.2+)
- Порты: 443 (HTTPS)

## 🔧 Параметры установки

### Версия Python
- **По умолчанию:** Python 3.13.1
- **Минимальная:** Python 3.11+
- **Рекомендуемая:** Последняя стабильная 3.13.x

### Параметры установщика
```
/quiet                  - Тихая установка без GUI
InstallAllUsers=1       - Установка для всех пользователей
PrependPath=1           - Добавление Python в PATH
Include_test=0          - Не устанавливать тестовые модули
Include_launcher=1      - Установить Python Launcher
SimpleInstall=1         - Упрощенная установка
```

### Место установки
**Для всех пользователей:**
```
C:\Program Files\Python313\
```

## 📖 Как это работает

### Процесс установки

1. **Проверка прав администратора**
   - Проверка наличия прав
   - UAC prompt при необходимости

2. **Определение архитектуры**
   - Автоматическое определение x86/x64/ARM64

3. **Определение версии**
   - Python 3.13.1 (конфигурируется в скрипте)

4. **Запрос подтверждения**
   - Показ параметров установки
   - Подтверждение пользователя (Y/N)

5. **Установка через winget** (приоритет)
   - Проверка наличия winget
   - Попытка установки через winget
   - Если не удалось → переход к шагу 6

6. **Альтернативный метод** (fallback)
   - Скачивание установщика с python.org
   - Установка с заданными параметрами

7. **Проверка установки**
   - Проверка `python --version`
   - Проверка `pip --version`
   - Обновление PATH в текущей сессии

### Откат при ошибке

Если установка завершается с ошибкой:
- Запуск деинсталлятора `/uninstall /quiet`
- Удаление временных файлов
- Очистка частично установленных компонентов

## 🔌 API для интеграции

### Переменные окружения

**Входные:**
- `CALLED_FROM_PARENT` - Установите в `1`, чтобы скрипт вернулся в родительский после установки

**Выходные:**
- Exit code `0` - Успешная установка
- Exit code `1` - Ошибка или отмена пользователем

### Примеры интеграции

#### Пример 1: Простая проверка и установка

**Batch:**
```batch
@echo off
python --version >nul 2>&1
if errorlevel 1 (
    call "python-installer\install-python.bat"
)
```

**PowerShell:**
```powershell
try {
    python --version 2>&1 | Out-Null
} catch {
    & "python-installer\install-python.ps1"
}
```

#### Пример 2: С обработкой ошибок

**Batch:**
```batch
@echo off
python --version >nul 2>&1
if errorlevel 1 (
    echo Python не найден, начинаем установку...
    set "CALLED_FROM_PARENT=1"
    call "%~dp0python-installer\install-python.bat"

    if errorlevel 1 (
        echo ОШИБКА: Не удалось установить Python
        echo Установите Python вручную: https://python.org
        pause
        exit /b 1
    )

    echo Python успешно установлен, продолжаем...
)

REM Ваш код, требующий Python
python -m pip install -r requirements.txt
```

**PowerShell:**
```powershell
try {
    $null = & python --version 2>&1
    if ($LASTEXITCODE -ne 0) { throw "Python не найден" }
} catch {
    Write-Host "Python не найден, начинаем установку..." -ForegroundColor Yellow
    $env:CALLED_FROM_PARENT = "1"
    & "$PSScriptRoot\python-installer\install-python.ps1"

    if ($LASTEXITCODE -ne 0) {
        Write-Host "ОШИБКА: Не удалось установить Python" -ForegroundColor Red
        Write-Host "Установите Python вручную: https://python.org"
        Read-Host "Нажмите Enter для выхода"
        exit 1
    }

    Write-Host "Python успешно установлен, продолжаем..." -ForegroundColor Green
}

# Ваш код, требующий Python
python -m pip install -r requirements.txt
```

#### Пример 3: С диалогом выбора

**Batch:**
```batch
@echo off
python --version >nul 2>&1
if errorlevel 1 (
    echo ================================
    echo Python не найден
    echo ================================
    echo.
    echo Выберите действие:
    echo   [1] Автоматическая установка Python
    echo   [2] Продолжить без установки (может привести к ошибкам)
    echo   [3] Выход
    echo.
    set /p "CHOICE=Ваш выбор (1-3): "

    if "!CHOICE!"=="1" (
        set "CALLED_FROM_PARENT=1"
        call "%~dp0python-installer\install-python.bat"
    ) else if "!CHOICE!"=="3" (
        exit /b 0
    )
)
```

## 🎨 Кастомизация

### Изменение версии Python

Откройте скрипт и измените переменные:

**install-python.bat:**
```batch
set "PYTHON_VERSION=3.12.7"
set "PYTHON_MAJOR=3.12"
```

**install-python.ps1:**
```powershell
$pythonVersion = "3.12.7"
$pythonMajor = "3.12"
```

### Изменение параметров установки

**install-python.bat (строка 146):**
```batch
"%INSTALLER_PATH%" /quiet InstallAllUsers=0 PrependPath=1 Include_test=0
```

**install-python.ps1 (строки 201-208):**
```powershell
$installArgs = @(
    "/quiet",
    "InstallAllUsers=0",  # 0 = только для текущего пользователя
    "PrependPath=1",
    "Include_test=0"
)
```

### Установка в пользовательский каталог

```batch
REM Для установки в конкретный каталог
"%INSTALLER_PATH%" /quiet TargetDir="C:\MyPython" PrependPath=1
```

## 🐛 Устранение неполадок

### Python не обнаружен после установки

**Проблема:** PATH не обновлен в текущей сессии

**Решение:**
1. Закройте командную строку / PowerShell
2. Откройте новое окно
3. Проверьте: `python --version`

### Ошибка скачивания установщика

**Проблема:** Проблемы с сетью или блокировка firewall

**Решение:**
1. Проверьте подключение к интернету
2. Проверьте настройки firewall
3. Попробуйте использовать VPN
4. Установите Python вручную

### winget не найден

**Информация:** Windows Package Manager не установлен

**Действие:**
- Скрипт автоматически перейдет к альтернативному методу (скачивание)
- Или установите winget: Microsoft Store → "App Installer"

### Антивирус блокирует установку

**Проблема:** Ложное срабатывание антивируса

**Решение:**
1. Временно отключите антивирус
2. Добавьте исключение для `python-installer.exe`
3. Скачайте установщик вручную и проверьте хеш

## 🔐 Безопасность

### Что проверяется
- ✅ Скачивание только с официального python.org
- ✅ Использование HTTPS (TLS 1.2+)
- ✅ Проверка URL на корректность
- ✅ Удаление временных файлов

### Что НЕ проверяется
- ❌ Цифровая подпись установщика (можно добавить)
- ❌ Хеш-сумма файла (можно добавить)

### Рекомендации
1. Запускайте скрипты только из доверенных источников
2. Проверяйте содержимое перед запуском
3. Используйте антивирус
4. Не отключайте UAC

## 📝 Лицензия

Этот модуль распространяется под лицензией MIT. Вы можете свободно использовать его в своих проектах.

## 🤝 Использование в других проектах

Этот модуль создан для переиспользования. Чтобы добавить его в свой проект:

1. **Скопируйте каталог:**
   ```bash
   cp -r python-installer /path/to/your/project/
   ```

2. **Интегрируйте вызов:**
   Используйте примеры из раздела "API для интеграции"

3. **Опционально:** Измените версию Python под ваши нужды

4. **Опционально:** Добавьте в `.gitignore`:
   ```
   # Python installer temp files
   python-installer/*.exe
   python-installer/*.log
   ```

## 📚 Дополнительные ресурсы

### Официальная документация Python
- [Установка на Windows](https://docs.python.org/3/using/windows.html)
- [Параметры установщика](https://docs.python.org/3/using/windows.html#installing-without-ui)

### Windows Package Manager (winget)
- [Документация winget](https://learn.microsoft.com/en-us/windows/package-manager/winget/)

### Python releases
- [Скачать Python](https://www.python.org/downloads/)
- [Release notes](https://www.python.org/downloads/release/)

## 📊 Статистика

- **Версия модуля:** 1.0.0
- **Поддерживаемые версии Python:** 3.11+
- **Тестировано на:** Windows 10 21H2+, Windows 11, Windows Server 2019+
- **Размер модуля:** < 50 КБ
- **Размер скачиваемого установщика:** ~30-40 МБ

## 🔄 История изменений

### 1.0.0 (2025-12-02)
- ✨ Первый релиз
- ✅ Поддержка x86, x64, ARM64
- ✅ Комбинированный метод установки (winget + direct download)
- ✅ UAC elevation
- ✅ Автоматический откат при ошибках
- ✅ Batch и PowerShell версии

---

**Создано для:** [fastlogcheck](https://github.com/your-org/fastlogcheck)
**Автор:** Your Name
**Дата:** 2025-12-02
