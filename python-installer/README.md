# Python Auto-Installer для Windows

Универсальные скрипты для автоматической установки Python на Windows.

## 🚀 Быстрый старт

### Standalone использование

**Интерактивный режим (с запросами):**

```cmd
install-python.bat
```

**Автоматический режим (без запросов):**

```cmd
install-python.bat /quiet
```

<details>
<summary>📘 Альтернатива: PowerShell версия (для опытных пользователей)</summary>

Используйте PowerShell версию, если:

- У вас заблокированы `.bat` файлы корпоративной политикой безопасности
- Вам нужна возможность цифровой подписи скриптов
- Вы предпочитаете PowerShell для интеграции с другими PowerShell скриптами

**Использование:**

```powershell
# Интерактивный режим
powershell -ExecutionPolicy Bypass -File install-python.ps1

# Автоматический режим
powershell -ExecutionPolicy Bypass -File install-python.ps1 -Quiet
```

</details>

### Интеграция в свой проект

Просто вызовите скрипт с параметром `/quiet` и проверьте код возврата:

```batch
REM Автоматическая установка Python при необходимости
call "%~dp0python-installer\install-python.bat" /quiet

if errorlevel 1 (
    echo Ошибка: Python не удалось установить
    exit /b 1
)

REM Продолжаем работу с Python
echo Python готов к работе
```

<details>
<summary>Интеграция из PowerShell скрипта</summary>

```powershell
# Автоматическая установка Python при необходимости
& "$PSScriptRoot\python-installer\install-python.ps1" -Quiet

if ($LASTEXITCODE -ne 0) {
    Write-Host "Ошибка: Python не удалось установить"
    exit 1
}

# Продолжаем работу с Python
Write-Host "Python готов к работе"
```

</details>

> **Примечание:** Установщик автоматически проверит наличие Python и установит его только при необходимости. В автоматическом режиме (`/quiet`) установка происходит полностью без запросов.

## 🔧 Параметры установки

### Версия Python

- **По умолчанию:** Python 3.14.0
- **Минимальная:** Python 3.11+
- **Рекомендуемая:** Последняя стабильная 3.14.x

### Параметры вызова

- **`/quiet`** (для bat) или **`-Quiet`** (для PowerShell) - Полностью автоматическая установка:
  - ✅ Без запросов подтверждения
  - ✅ Без пауз
  - ✅ Установка для текущего пользователя (не требует прав администратора)
  - ✅ Автоматическое добавление в PATH
  - ✅ Возвращает код 0 при успехе, 1 при ошибке

<details>
<summary>Устаревший параметр CALLED_FROM_PARENT (не рекомендуется)</summary>

- **`CALLED_FROM_PARENT=1`** - Подавляет только паузы, но скрипт остается интерактивным (требует подтверждения Y/N).

> 💡 Вместо этого используйте `/quiet` - он полностью автоматический.

</details>

### Параметры установщика Python

```
/quiet                  - Тихая установка без GUI
InstallAllUsers=0/1     - 0 = текущий пользователь, 1 = все пользователи
PrependPath=1           - Добавление Python в PATH
Include_test=0          - Не устанавливать тестовые модули
Include_launcher=1      - Установить Python Launcher
SimpleInstall=1         - Упрощенная установка
```

### Место установки

**Вариант 1 (по умолчанию) - Для текущего пользователя:**

- **Не требует прав администратора**
- Путь: `%LOCALAPPDATA%\Programs\Python\Python314`
- Доступен только для текущей учетной записи

**Вариант 2 - Для всех пользователей:**

- **Требует права администратора** (будет запрошена элевация)
- Путь: `C:\Program Files\Python314`
- Доступен для всех пользователей системы

## 📖 Как это работает

### Процесс установки

1. Проверка наличия Python в PATH
2. Поиск Python в стандартных местах установки
3. Выбор типа установки (текущий пользователь / все пользователи)
4. Запрос элевации прав **только при установке для всех пользователей**
5. Автоматическое определение архитектуры (x86/x64/ARM64)
6. Выбор версии Python (3.14.0 по умолчанию)
7. Подтверждение параметров установки (Y/N)
8. Попытка установки через winget (если доступен)
9. Если winget не удался → скачивание с python.org
10. Проверка доступности python и pip
11. Обновление PATH в текущей сессии

### Откат при ошибке

Если установка завершается с ошибкой:

- Запуск деинсталлятора `/uninstall /quiet`
- Удаление временных файлов
- Очистка частично установленных компонентов

### Коды возврата

**Выходные:**

- Exit code `0` - Успешная установка
- Exit code `1` - Ошибка или отмена пользователем
