# 🚀 Быстрый старт

## Установка (5 минут)

```bash
# 1. Распаковать архив
tar -xzf monitoring.tar.gz
cd monitoring

# 2. Установить зависимости
./install.sh

# 3. Проверить работу (замените srv-hv4 на ваш хост)
./check_server_logs.py srv-hv4
```

## Первый запуск

```bash
# Базовая проверка одного сервера
./check_server_logs.py srv-hv4

# Результат: report_srv-hv4_2025-12-01_19-05.html
```

## С автоочисткой ZFS

```bash
# Автоматически очистить ZFS если заполнено > 85%
./check_server_logs.py srv-hv4 --cleanup-threshold 85

# Скрипт удалит до 50 снимков (5 итераций × 10 снимков)
# исключая снимки с меткой "replicate"
```

## Проверка нескольких серверов

```bash
# Создаёт отдельный отчёт для каждого сервера
./check_server_logs.py srv-hv1 srv-hv2 srv-hv4
```

## Настройка SSH

### Вариант 1: Использовать существующий SSH конфиг

```bash
./check_server_logs.py srv-hv4 --ssh-config ~/.claude/ssh/config
```

### Вариант 2: Настроить SSH ключи вручную

```bash
# Скопировать SSH ключ на сервер
ssh-copy-id root@srv-hv4

# Проверить подключение
ssh root@srv-hv4 "hostname"

# Запустить скрипт
./check_server_logs.py srv-hv4
```

## Частые сценарии

### Ежедневная проверка в 08:00

```bash
# Создать скрипт
cat > daily_check.sh << 'EOF'
#!/bin/bash
cd /opt/monitoring
./check_server_logs.py srv-hv1 srv-hv2 srv-hv4 \
  --cleanup-threshold 85 \
  --output /var/www/html/reports/report_$(date +%Y-%m-%d).html
EOF

chmod +x daily_check.sh

# Добавить в cron
crontab -e
# Добавить строку:
# 0 8 * * * /opt/monitoring/daily_check.sh
```

### Анализ за 7 дней с детальным выводом

```bash
./check_server_logs.py srv-hv4 \
  --period 168 \
  --verbose \
  --json
```

### Проверка медленного сервера

```bash
./check_server_logs.py srv-hv4 \
  --ssh-timeout 60 \
  --parallel 2
```

## Структура проекта

```
monitoring/
├── check_server_logs.py       # Основной скрипт (исполняемый)
├── requirements.txt            # Python зависимости
├── install.sh                  # Скрипт установки (исполняемый)
├── README.md                   # Полная документация
├── QUICKSTART.md              # Этот файл
├── templates/
│   └── report_template.html   # Jinja2 шаблон отчёта
└── config/
    └── servers_example.conf   # Пример SSH конфига
```

## Результаты работы

После запуска скрипта вы получите:

1. **HTML отчёт** - `report_HOSTNAME_DATE_TIME.html`
   - Интерактивная таблица
   - Кликабельные строки
   - Цветовая индикация

2. **Лог-файл** - `check_server_logs.log`
   - Детальная информация о выполнении
   - Ошибки и предупреждения

3. **JSON файл** (если --json) - `report_HOSTNAME_DATE_TIME.json`
   - Структурированные данные
   - Для автоматической обработки

## Помощь

```bash
# Показать все параметры
./check_server_logs.py --help

# Подробная документация
cat README.md
```

## Что проверяется

- ✅ Системные ошибки (journalctl)
- ✅ Неудачные попытки входа (auth.log)
- ✅ Заблокированные IP (fail2ban)
- ✅ Проблемы кластера (corosync)
- ✅ Ошибки ядра (dmesg)
- ✅ HTTP ошибки (pveproxy)
- ✅ Статус VM (qm list)
- ✅ Заполненность дисков (pvesm)
- ✅ Кворум кластера (pvecm)
- ✅ ZFS снимки (опционально)

## Устранение проблем

### "Ошибка подключения SSH"
```bash
# Проверить SSH вручную
ssh root@srv-hv4

# Скопировать ключ
ssh-copy-id root@srv-hv4
```

### "Permission denied"
```bash
# Убедиться что пользователь root
./check_server_logs.py srv-hv4 --ssh-user root
```

### "Timeout"
```bash
# Увеличить timeout
./check_server_logs.py srv-hv4 --ssh-timeout 60
```

## Что дальше?

1. Прочитайте полную документацию: `cat README.md`
2. Настройте ежедневную проверку через cron
3. Добавьте свои проверки (см. раздел "Разработка" в README)
4. Интегрируйте с системами мониторинга

---

**Вопросы?** См. полную документацию в README.md
